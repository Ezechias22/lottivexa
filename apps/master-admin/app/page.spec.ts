import{readFileSync}from'node:fs';import{describe,expect,it}from'vitest';const source=readFileSync(new URL('./page.tsx',import.meta.url),'utf8');describe('master admin console',()=>{it('uses platform authentication',()=>{expect(source).toContain("tenant:'platform'");expect(source).toContain('/auth/login')});it('connects tenant and subscription mutations',()=>{for(const endpoint of['/tenants','/subscriptions/activate','/subscriptions/${r.id}/extend','/subscriptions/${r.id}/change-plan','/billing/payments/','/plans'])expect(source).toContain(endpoint)});it('manages database-backed plan feature flags',()=>{expect(source).toContain('PLAN_FEATURES');expect(source).toContain('/features/${key}');expect(source).toContain('{enabled:!enabled}')});it('does not render fabricated dashboard values',()=>{expect(source).toContain('/master/dashboard');expect(source).not.toMatch(/Total tenants','—|fake/i)})})
