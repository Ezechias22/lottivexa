import'./language.css';import type{Metadata}from'next';import type{ReactNode}from'react';import{LanguageSwitcher}from'./language-switcher';export const metadata:Metadata={title:'Lottivexa Merchant POS',description:'Fast lottery and bolet point of sale'};export default function Layout({children}:{children:ReactNode}){return <html lang="ht"><body><LanguageSwitcher/>{children}</body></html>}
