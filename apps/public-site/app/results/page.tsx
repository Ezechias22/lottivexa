'use client';
import{FormEvent,useEffect,useState}from'react';
const RAW_API=process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000';const API=RAW_API.replace(/\/$/,'').endsWith('/api/v1')?RAW_API.replace(/\/$/,''):RAW_API.replace(/\/$/,'')+'/api/v1';type Row=Record<string,any>;
export default function Results(){const[tenant,setTenant]=useState(''),[data,setData]=useState<Row>({results:[]}),[error,setError]=useState('');async function load(slug=tenant){if(!slug)return;try{const response=await fetch(`${API}/results/public/${encodeURIComponent(slug)}`,{cache:'no-store'}),body=await response.json();if(!response.ok)throw new Error(body.message??'RESULTS_UNAVAILABLE');setData(body);setError('')}catch(e){setError(e instanceof Error?e.message:String(e))}}useEffect(()=>{const slug=new URLSearchParams(location.search).get('tenant')??'';setTenant(slug);if(slug){void load(slug);const timer=setInterval(()=>void load(slug),15000);return()=>clearInterval(timer)}},[]);return <main style={{fontFamily:'system-ui',maxWidth:1000,margin:'auto',padding:24}}><header style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:16}}><div><b>LOTTIVEXA</b><h1>{data.tenant?.businessName??'Rezilta lotri'}</h1></div><span style={{color:'#16803a'}}>● AN TAN REYÈL</span></header><form onSubmit={(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();void load()}} style={{display:'flex',gap:8,margin:'20px 0'}}><input aria-label="Tenant" value={tenant} onChange={e=>setTenant(e.target.value)} placeholder="non tenant lan" required style={{padding:12,flex:1}}/><button style={{padding:'12px 20px'}}>Afiche rezilta</button></form>{error&&<p>{error}</p>}<section style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(240px,1fr))',gap:14}}>{(data.results??[]).map((row:Row)=><article key={row.id} style={{border:'1px solid #ddd',borderRadius:14,padding:18}}><small>{row.game?.catalogCode}</small><h2>{row.game?.name}</h2>{row.game?.logoUrl&&<img src={row.game.logoUrl} alt={`Logo `} style={{height:64,maxWidth:180,objectFit:"contain"}}/>}<b>{row.drawNumber}</b><p style={{fontSize:24,fontWeight:900}}>{row.result?.winningKeys?.join(' · ')??'—'}</p><time>{row.publishedAt?new Date(row.publishedAt).toLocaleString():'—'}</time></article>)}</section>{!data.results?.length&&!error&&<p>Pa gen rezilta pibliye.</p>}</main>}
