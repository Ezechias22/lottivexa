'use client';
import{FormEvent,useCallback,useEffect,useMemo,useState}from'react';
import'./styles.css';
import DeviceActions from'./device-actions';
import MerchantActions from'./merchant-actions';
import UserActions from'./user-actions';import ManualResults from'./manual-results';import{localizedColumn,useI18n}from'./i18n';import{drawSessionLabel}from'./draw-label';
const RAW_API=process.env.NEXT_PUBLIC_API_URL??'http://localhost:4000';const API=RAW_API.replace(/\/$/,'').endsWith('/api/v1')?RAW_API.replace(/\/$/,''):RAW_API.replace(/\/$/,'')+'/api/v1';
const RESET_URL=process.env.NEXT_PUBLIC_PASSWORD_RESET_URL??'https://lottivexa-public-site.onrender.com/reset-password';
type Row=Record<string,any>;type Tab='dashboard'|'branches'|'merchants'|'users'|'lottery'|'tickets'|'finance'|'devices'|'printers'|'reports'|'branding'|'audit';
const NAV:{id:Tab;label:string;permission?:string;feature?:string}[]=[{id:'dashboard',label:'Dashboard'},{id:'tickets',label:'Tickets',permission:'tickets.view'},{id:'lottery',label:'Games & Draws',permission:'tickets.view'},{id:'merchants',label:'Merchants',permission:'merchants.view'},{id:'branches',label:'Branches',permission:'branches.view'},{id:'users',label:'Users & Roles',permission:'users.view'},{id:'finance',label:'Finance',permission:'finance.view',feature:'finance'},{id:'devices',label:'Devices',permission:'devices.view',feature:'device_management'},{id:'printers',label:'Printers',permission:'printers.view'},{id:'reports',label:'Reports',permission:'reports.view'},{id:'branding',label:'Settings & Domains',permission:'settings.view'},{id:'audit',label:'Audit Logs',permission:'audit.view',feature:'audit_logs'}];
function claims(token:string){try{return JSON.parse(atob(token.split('.')[1].replace(/-/g,'+').replace(/_/g,'/')))}catch{return{permissions:[]}}}
function money(v:any){return new Intl.NumberFormat('fr-HT',{minimumFractionDigits:2,maximumFractionDigits:2}).format(Number(v??0))}
function haitiToday(value=new Date()){const parts=Object.fromEntries(new Intl.DateTimeFormat('en-US',{timeZone:'America/Port-au-Prince',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(value).map(x=>[x.type,x.value]));return `${parts.year}-${parts.month}-${parts.day}`}
function shiftDate(date:string,days:number){const[y,m,d]=date.split('-').map(Number);return new Date(Date.UTC(y,m-1,d+days)).toISOString().slice(0,10)}
function val(form:HTMLFormElement){return Object.fromEntries(new FormData(form))as Row}function uuid(){return crypto.randomUUID()}
function Table({rows,columns,actions}:{rows:Row[];columns:[string,string][];actions?:(r:Row)=>React.ReactNode}){const{language,t}=useI18n();return <div className="table-wrap"><table><thead><tr>{columns.map(c=><th key={c[0]}>{localizedColumn(c[1],language)}</th>)}{actions&&<th>{t('table.actions')}</th>}</tr></thead><tbody>{rows.length?rows.map((r,i)=><tr key={r.id??i}>{columns.map(c=><td key={c[0]}>{String(c[0].split('.').reduce((x:any,k)=>x?.[k],r)??'—')}</td>)}{actions&&<td className="actions">{actions(r)}</td>}</tr>):<tr><td colSpan={columns.length+(actions?1:0)} className="empty">{t('table.empty')}</td></tr>}</tbody></table></div>}
export default function TenantConsole(){
 const[token,setToken]=useState(''),[refresh,setRefresh]=useState(''),[force,setForce]=useState(false),[tab,setTab]=useState<Tab>('dashboard'),[data,setData]=useState<Record<string,any>>({}),[features,setFeatures]=useState<string[]>([]),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);
 const permissions=useMemo<string[]>(()=>claims(token).permissions??[],[token]);const can=(p:string)=>permissions.includes(p),has=(f:string)=>features.includes(f);
 useEffect(()=>{setToken(localStorage.getItem('tenant_access')??'');setRefresh(localStorage.getItem('tenant_refresh')??'');setForce(localStorage.getItem('tenant_force_password')==='true')},[]);
 const logout=useCallback(()=>{localStorage.removeItem('tenant_access');localStorage.removeItem('tenant_refresh');localStorage.removeItem('tenant_force_password');setToken('');setRefresh('');setData({});setFeatures([])},[]);
 const request=useCallback(async(path:string,init:RequestInit={})=>{let access=localStorage.getItem('tenant_access')??token;const send=()=>fetch(`${API}${path}`,{...init,headers:{...(!(init.body instanceof FormData)?{'content-type':'application/json'}:{}),authorization:`Bearer ${access}`,...init.headers}});let response=await send();if(response.status===401&&refresh){const renewed=await fetch(`${API}/auth/refresh`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({refreshToken:refresh})});if(renewed.ok){const r=await renewed.json();access=r.accessToken;localStorage.setItem('tenant_access',r.accessToken);localStorage.setItem('tenant_refresh',r.refreshToken);setToken(r.accessToken);setRefresh(r.refreshToken);response=await send()}else logout()}const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.code??body.message??`HTTP_${response.status}`);return body},[token,refresh,logout]);
 const run=useCallback(async(action:()=>Promise<any>,success?:string)=>{setBusy(true);setMessage('');try{const result=await action();if(success)setMessage(success);return result}catch(e){setMessage(e instanceof Error?e.message:String(e))}finally{setBusy(false)}},[]);
 const load=useCallback(async(current:Tab=tab)=>{const endpoints:Record<Tab,string[]>={dashboard:[`/reports/sales?from=${haitiToday()}&to=${haitiToday()}`,'/tickets','/branches','/merchants','/notifications?unread=true'],branches:['/branches'],merchants:['/merchants','/branches'],users:['/users','/roles','/permissions'],lottery:['/lottery/games','/lottery/draws'],tickets:['/tickets','/lottery/draws'],finance:['/finance/accounts','/finance/ledger','/finance/trial-balance','/cash/sessions'],devices:['/devices','/branches','/merchants'],printers:['/printing/printers','/branches','/printing/templates','/printing/receipt-branding'],reports:[`/reports/sales?from=${shiftDate(haitiToday(),-29)}&to=${haitiToday()}`,`/reports/draws?from=${shiftDate(haitiToday(),-29)}&to=${haitiToday()}`],branding:['/settings','/domains'],audit:['/audit?limit=100']};const values=await run(()=>Promise.all(endpoints[current].map(x=>request(x))));if(values)setData(old=>({...old,[current]:values}))},[tab,request,run]);
 useEffect(()=>{if(token&&!force)void request('/settings').then(s=>setFeatures((s.subscription?.plan?.features??[]).filter((x:Row)=>x.enabled).map((x:Row)=>x.key))).catch(()=>setFeatures([]))},[token,force,request]);
 useEffect(()=>{if(token&&!force)void load(tab)},[tab,token,force]);
 async function login(e:FormEvent<HTMLFormElement>){e.preventDefault();const body=val(e.currentTarget);const result=await run(async()=>{const response=await fetch(`${API}/auth/login`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)}),payload=await response.json();if(!response.ok)throw new Error(payload.code??payload.message??'INVALID_CREDENTIALS');return payload},'Koneksyon reyisi.');if(result){localStorage.setItem('tenant_access',result.accessToken);localStorage.setItem('tenant_refresh',result.refreshToken);localStorage.setItem('tenant_force_password',String(result.forcePasswordChange));setToken(result.accessToken);setRefresh(result.refreshToken);setForce(result.forcePasswordChange)}}
 async function submit(e:FormEvent<HTMLFormElement>,path:string,transform:(x:Row)=>any=x=>x,method='POST',success='Operasyon an reyisi.'){e.preventDefault();const form=e.currentTarget,result=await run(()=>request(path,{method,body:JSON.stringify(transform(val(form)))}),success);if(result){form.reset();await load(tab)}}
 if(!token)return <main className="login"><div className="logo">LOTTIVEXA</div><h1>Tenant Admin</h1><p className="muted">Antre nan espas biznis ou. Pa gen enskripsyon merchant.</p><form onSubmit={login}><label>Tenant / subdomain<input name="tenant" required/></label><label>Username / phone<input name="username" required/></label><label>Password<input name="password" type="password" minLength={8} required/></label><button disabled={busy}>Login</button></form><a href={RESET_URL}>Forgot password?</a>{message&&<p className="message">{message}</p>}</main>;
 if(force)return <main className="login"><div className="logo">LOTTIVEXA</div><h1>Chanje modpas tanporÃ¨</h1><p>Ou dwe mete yon nouvo modpas avan ou antre nan sistÃ¨m nan.</p><form onSubmit={async e=>{e.preventDefault();const form=e.currentTarget,result=await run(()=>request('/users/me/change-password',{method:'POST',body:JSON.stringify(val(form))}),'Modpas chanje. Konekte ankÃ².');if(result)setTimeout(logout,800)}}><label>Modpas aktyÃ¨l<input name="currentPassword" type="password" required/></label><label>Nouvo modpas<input name="newPassword" type="password" minLength={12} required/></label><button>Chanje modpas</button></form><button className="secondary" onClick={logout}>Retounen login</button>{message&&<p className="message">{message}</p>}</main>;
 const current=data[tab]??[];
 return <div className="shell"><aside><div className="logo">LOTTIVEXA</div>{NAV.filter(n=>(!n.permission||can(n.permission))&&(!n.feature||has(n.feature))).map(n=><button key={n.id} className={tab===n.id?'active':''} onClick={()=>setTab(n.id)}>{n.label}</button>)}<button onClick={logout}>Logout</button></aside><main className="content"><header><div><small>TENANT CONSOLE</small><h1>{NAV.find(n=>n.id===tab)?.label}</h1></div><button className="secondary" onClick={()=>load(tab)} disabled={busy}>â†» Reload</button></header>{message&&<p className="message">{message}</p>}{tab==='dashboard'&&<Dashboard data={current}/>} {tab==='branches'&&<Branches data={current} submit={submit} can={can}/>} {tab==='merchants'&&<Merchants data={current} submit={submit} request={request} load={load} can={can}/>} {tab==='users'&&<Users data={current} submit={submit} request={request} load={load} can={can} has={has}/>} {tab==='lottery'&&<Lottery data={current} submit={submit} request={request} load={load} can={can}/>} {tab==='tickets'&&<Tickets data={current}/>} {tab==='finance'&&<Finance data={current} submit={submit}/>} {tab==='devices'&&<Devices data={current} submit={submit} request={request} load={load} can={can}/>} {tab==='printers'&&<Printers data={current} submit={submit} has={has}/>} {tab==='reports'&&<Reports data={current} request={request} run={run} token={token}/>} {tab==='branding'&&<Branding data={current} submit={submit} request={request} load={load} has={has}/>} {tab==='audit'&&<Audit data={current}/>}</main></div>
}
function Dashboard({data:d}:{data:any[]}){const{t,language}=useI18n();const[r={},tickets=[],branches=[],merchants=[],notes=[]]=d;const today=new Intl.DateTimeFormat(language==='fr'?'fr-FR':'fr-HT',{timeZone:'America/Port-au-Prince',dateStyle:'full'}).format(new Date());return <><section className="dashboard-welcome"><div><span className="eyebrow">{t('dashboard.today')}</span><h2>{today}</h2><p>{t('report.fromToday')}</p></div><span className="today-mark">{t('dashboard.today')}</span></section><div className="cards"><article className="metric-sales"><span>{t('dashboard.today')}</span><strong>{money(r.tickets?.sales)}</strong><small>{r.tickets?.count??tickets.length} {t('dashboard.tickets').toLowerCase()}</small></article><article><span>{t('dashboard.payouts')}</span><strong>{money(r.payouts?.amount)}</strong><small>{r.payouts?.count??0} {t('report.tickets').toLowerCase()}</small></article><article><span>{t('dashboard.net')}</span><strong>{money(Number(r.tickets?.sales??0)-Number(r.payouts?.amount??0))}</strong></article><article><span>{t('dashboard.tickets')}</span><strong>{r.tickets?.count??0}</strong></article><article><span>{t('dashboard.branches')}</span><strong>{branches.length}</strong></article><article><span>{t('dashboard.merchants')}</span><strong>{merchants.length}</strong></article><article><span>{t('dashboard.alerts')}</span><strong>{notes.length}</strong></article></div><section className="panel"><h2>{t('dashboard.recent')}</h2><Table rows={tickets.slice(0,12)} columns={[["ticketNumber","Ticket"],["status","Status"],["amount","Amount"],["createdAt","Date"]]}/></section></>}
function Branches({data:d,submit,can}:any){return <><section className="panel"><h2>Create branch</h2>{can('branches.create')&&<form className="form" onSubmit={e=>submit(e,'/branches',(x:Row)=>({...x,openingHours:{note:x.openingHours}}))}>{[['code','Code'],['name','Name'],['address','Address'],['phone','Phone'],['openingHours','Opening hours']].map(x=><label key={x[0]}>{x[1]}<input name={x[0]} required={x[0]==='code'||x[0]==='name'}/></label>)}<button>Create</button></form>}</section><section className="panel"><Table rows={d[0]??[]} columns={[['code','Code'],['name','Name'],['address','Address'],['phone','Phone'],['_count.merchants','Merchants'],['status','Status']]}/></section></>}
function Merchants({data:d,submit,request,load,can}:any){const[rows=[],branches=[]]=d;return <><section className="panel"><h2>Create merchant account</h2><p className="muted">Merchant la pa ka enskri tÃ¨t li; admin lan kreye login sa a.</p>{can('merchants.create')&&<form className="form" onSubmit={e=>submit(e,'/merchants')}><label>Display name<input name="displayName" required/></label><label>Merchant number<input name="merchantNumber" required/></label><label>Username<input name="username" required/></label><label>Phone<input name="phone"/></label><label>Email<input name="email" type="email"/></label><label>Temporary password<input name="temporaryPassword" type="password" minLength={12} required/></label><label>Branch<select name="branchId" required><option value="">Select</option>{branches.map((b:Row)=><option key={b.id} value={b.id}>{b.name}</option>)}</select></label><button>Create merchant</button></form>}</section><section className="panel"><Table rows={rows} columns={[['merchantNumber','Number'],['displayName','Name'],['user.username','Login'],['branch.name','Branch'],['status','Status']]} actions={(r)=><MerchantActions row={r} request={request} load={load} can={can}/>}/></section></>}
function Users({data:d,submit,request,load,can,has}:any){const[rows=[],roles=[],permissions=[]]=d;return <><div className="split"><section className="panel"><h2>Create user</h2>{can('users.create')&&<form className="form one" onSubmit={e=>submit(e,'/users',(x:Row)=>({...x,roleIds:[x.roleId]}))}><label>Username<input name="username" required/></label><label>Email<input name="email" type="email"/></label><label>Phone<input name="phone"/></label><label>Temporary password<input name="temporaryPassword" type="password" minLength={12} required/></label><label>Role<select name="roleId" required>{roles.map((r:Row)=><option key={r.id} value={r.id}>{r.name}</option>)}</select></label><button>Create user</button></form>}</section><section className="panel"><h2>Create granular role</h2>{can('users.create')&&has('advanced_permissions')&&<form className="form one" onSubmit={e=>submit(e,'/roles',(x:Row)=>({code:x.code,name:x.name,permissions:String(x.permissions).split(',').map(v=>v.trim()).filter(Boolean)}))}><label>Role code<input name="code" placeholder="PRINT_WORKER" pattern="[A-Z][A-Z0-9_]{2,79}" required/></label><label>Name<input name="name" placeholder="Printing Worker" required/></label><label>Permission codes (comma separated)<textarea name="permissions" defaultValue="printers.view, printers.configure" required/></label><button>Create role</button></form>}<details><summary>Available permission codes</summary><code className="permission-list">{permissions.map((p:Row)=>p.code).join(', ')}</code></details></section></div><section className="panel"><h2>Users</h2><Table rows={rows} columns={[["username","Username"],["email","Email"],["phone","Phone"],["status","Status"],["forcePasswordChange","Password change"]]} actions={(r)=><UserActions row={r} roles={roles} request={request} load={load} can={can}/>}/></section><section className="panel"><h2>Roles</h2><Table rows={roles.map((r:Row)=>({...r,permissionCount:r.permissions?.length??0}))} columns={[["code","Code"],["name","Name"],["permissionCount","Permissions"],["isSystem","System"]]}/></section></>}
function Lottery({data:d,submit,request,load,can}:any){const{language}=useI18n();const[games=[],draws=[]]=d;const labeledDraws=draws.map((draw:Row)=>({...draw,sessionLabel:drawSessionLabel(draw,language)}));return <><LotterySetup games={games} draws={draws} request={request} load={load} can={can}/><div className="split"><section className="panel"><h2>Create game</h2>{can('settings.edit')&&<form className="form one" onSubmit={e=>submit(e,'/lottery/games',(x:Row)=>({...x,cutoffSeconds:Number(x.cutoffSeconds),resultDigits:Number(x.resultDigits)}))}><label>Code<input name="code" required/></label><label>Name<input name="name" required/></label><label>Description<input name="description"/></label><label>Cutoff seconds<input name="cutoffSeconds" type="number" min="0" required/></label><label>Result digits<input name="resultDigits" type="number" min="1" max="20" required/></label><button>Create game</button></form>}</section><section className="panel"><h2>Create draw</h2>{can('settings.edit')&&<form className="form one" onSubmit={e=>submit(e,'/lottery/draws',(x:Row)=>({...x,drawDate:new Date(x.drawDate).toISOString(),opensAt:new Date(x.opensAt).toISOString(),closesAt:new Date(x.closesAt).toISOString(),resultAt:new Date(x.resultAt).toISOString()}))}><label>Game<select name="gameId" required>{games.map((g:Row)=><option key={g.id} value={g.id}>{g.name}</option>)}</select></label><label>Draw number<input name="drawNumber" required/></label><label>Draw date<input name="drawDate" type="date" required/></label><label>Opens at<input name="opensAt" type="datetime-local" required/></label><label>Closes at<input name="closesAt" type="datetime-local" required/></label><label>Result at<input name="resultAt" type="datetime-local" required/></label><button>Create draw</button></form>}</section></div><section className="panel"><h2>Games</h2><Table rows={games} columns={[['code','Code'],['name','Name'],['status','Status'],['cutoffSeconds','Cutoff'],['resultDigits','Digits']]}/></section><section className="panel"><h2>Draws</h2><Table rows={labeledDraws} columns={[['drawNumber','Draw'],['game.name','Game'],['sessionLabel','Session'],['status','Status'],['opensAt','Open'],['closesAt','Close']]} actions={can('settings.edit')?(r)=><>{r.status==='SCHEDULED'&&<button onClick={async()=>{await request(`/lottery/draws/${r.id}/transition`,{method:'POST',body:JSON.stringify({status:'OPEN'})});await load('lottery')}}>Open</button>}{r.status==='OPEN'&&<button onClick={async()=>{await request(`/lottery/draws/${r.id}/transition`,{method:'POST',body:JSON.stringify({status:'CLOSED'})});await load('lottery')}}>Close</button>}</>:undefined}/></section></>}
function Tickets({data:d}:{data:any[]}){const[tickets=[],draws=[]]=d;return <><div className="cards"><article><span>Total loaded</span><strong>{tickets.length}</strong></article><article><span>Open draws</span><strong>{draws.filter((x:Row)=>x.status==='OPEN').length}</strong></article><article><span>Winners</span><strong>{tickets.filter((x:Row)=>x.status==='WINNER').length}</strong></article><article><span>Paid</span><strong>{tickets.filter((x:Row)=>x.status==='PAID').length}</strong></article></div><section className="panel"><Table rows={tickets} columns={[['ticketNumber','Ticket'],['status','Status'],['amount','Amount'],['potentialWin','Potential win'],['createdAt','Date']]}/></section></>}
function Finance({data:d,submit}:any){const[accounts=[],ledger=[],balance=[],sessions=[]]=d;return <><div className="cards">{balance.slice(0,6).map((x:Row)=><article key={x.accountId}><span>{x.code??x.accountId}</span><strong>{money(x.balance)}</strong></article>)}</div><section className="panel"><h2>Accounts</h2><Table rows={accounts} columns={[['code','Code'],['name','Account'],['type','Type'],['currency','Currency']]}/></section><section className="panel"><h2>Cash sessions</h2><Table rows={sessions} columns={[['status','Status'],['merchant.displayName','Merchant'],['branch.name','Branch'],['openingCash','Opening'],['expectedCash','Expected'],['actualCash','Actual'],['differenceStatus','Difference'],['openedAt','Opened']]}/></section><section className="panel"><h2>Ledger</h2><Table rows={ledger} columns={[['reference','Reference'],['type','Type'],['description','Description'],['createdAt','Date']]}/></section></>}
function Devices({data:d,submit,request,load,can}:any){const[rows=[],branches=[],merchants=[]]=d;return <><section className="panel"><h2>Register device</h2><form className="form" onSubmit={e=>submit(e,'/devices',(x:Row)=>({...x,merchantId:x.merchantId||undefined}))}><label>Name<input name="name" required/></label><label>Branch<select name="branchId" required>{branches.map((b:Row)=><option key={b.id} value={b.id}>{b.name}</option>)}</select></label><label>Merchant<select name="merchantId"><option value="">Unassigned</option>{merchants.map((m:Row)=><option key={m.id} value={m.id}>{m.displayName}</option>)}</select></label><label>Type<input name="deviceType" placeholder="PHONE / TABLET / POS" required/></label><label>Platform<input name="platform" placeholder="ANDROID / IOS / WEB" required/></label><label>OS version<input name="osVersion"/></label><label>App version<input name="appVersion"/></label><button>Register</button></form></section><section className="panel"><Table rows={rows} columns={[['name','Name'],['platform','Platform'],['deviceType','Type'],['status','Status'],['lastSeenAt','Last seen']]} actions={r=><DeviceActions row={r} request={request} load={load} can={can}/>}/></section></>}
function Printers({data:d,submit,has}:any){const[rows=[],branches=[],templates=[],branding={}]=d,[kind,setKind]=useState('LOTTERY_MACHINE'),[preview,setPreview]=useState<Row>({name:'Default Ticket',templateKind:'TICKET',width:'58',header:'',footer:'',showBarcode:'true',showQr:'true'});const configuration=(x:Row)=>x.connectionType==='NETWORK'?{host:x.host,port:Number(x.port||9100)}:x.connectionType==='USB'?{devicePath:x.devicePath,vendorId:Number(x.vendorId||0),productId:Number(x.productId||0)}:x.connectionType==='BLUETOOTH'?{devicePath:x.devicePath,deviceAddress:x.deviceAddress}: {driver:x.driver,host:x.host||undefined,port:x.host?Number(x.port||9100):undefined,devicePath:x.devicePath||undefined};return <><div className="split"><section className="panel"><h2>Add printer</h2><form className="form one" onSubmit={e=>submit(e,'/printing/printers',(x:Row)=>({branchId:x.branchId,name:x.name,connectionType:x.connectionType,protocol:x.protocol,isDefault:x.isDefault==='true',configuration:configuration(x)}))}><label>Name<input name="name" required/></label><label>Branch<select name="branchId" required>{branches.map((b:Row)=><option key={b.id} value={b.id}>{b.name}</option>)}</select></label><label>Connection<select name="connectionType" value={kind} onChange={e=>setKind(e.target.value)}><option disabled={!has('bluetooth_printing')}>BLUETOOTH</option><option disabled={!has('usb_printing')}>USB</option><option disabled={!has('network_printing')}>NETWORK</option><option>LOTTERY_MACHINE</option></select></label><label>Protocol<input name="protocol" defaultValue="ESC_POS" required/></label>{kind==='NETWORK'&&<><label>Host / IP<input name="host" required/></label><label>Port<input name="port" type="number" defaultValue="9100" required/></label></>}{kind==='USB'&&<><label>Linux device path<input name="devicePath" defaultValue="/dev/usb/lp0" required/></label><label>Vendor ID<input name="vendorId" type="number" defaultValue="0"/></label><label>Product ID<input name="productId" type="number" defaultValue="0"/></label></>}{kind==='BLUETOOTH'&&<><label>RFCOMM device path<input name="devicePath" defaultValue="/dev/rfcomm0" required/></label><label>Bluetooth address<input name="deviceAddress" placeholder="AA:BB:CC:DD:EE:FF" required/></label></>}{kind==='LOTTERY_MACHINE'&&<><label>Adapter / driver<input name="driver" required/></label><label>Host (optional)<input name="host"/></label><label>Port<input name="port" type="number" defaultValue="9100"/></label><label>Device path (optional)<input name="devicePath"/></label></>}<label>Default<select name="isDefault"><option value="false">No</option><option value="true">Yes</option></select></label><button>Add printer</button></form></section><section className="panel"><h2>Ticket / receipt designer</h2><form className="form one" onChange={e=>setPreview(val(e.currentTarget))} onSubmit={e=>submit(e,'/printing/templates',(x:Row)=>({name:x.name,kind:x.templateKind,definition:{paperWidth:Number(x.width),header:x.header,footer:x.footer,showBarcode:x.showBarcode==='true',showQr:x.showQr==='true'}}))}><label>Template name<input name="name" defaultValue={preview.name} required/></label><label>Kind<select name="templateKind" defaultValue="TICKET"><option>TICKET</option><option>RECEIPT</option></select></label><label>Paper width<select name="width" defaultValue="58"><option value="58">58 mm</option><option value="80">80 mm</option></select></label><label>Header<input name="header"/></label><label>Footer<input name="footer"/></label><label>Barcode<select name="showBarcode" defaultValue="true"><option value="true">Show</option><option value="false">Hide</option></select></label><label>QR<select name="showQr" defaultValue="true"><option value="true">Show</option><option value="false">Hide</option></select></label><button>Save new version</button></form><div className={preview.width==='80'?'thermal wide':'thermal'}><b>{branding.businessName||'NON BIZNIS LA'}</b>{preview.header&&!/lottivexa/i.test(preview.header)&&<span>{preview.header}</span>}<span>TIKE LV-EXAMPLE</span><span>GAME / DRAW</span><span>12-34 ........ 10.00</span><hr/><span>TOTAL ......... 10.00</span>{preview.showBarcode!=='false'&&<span>|||| BARCODE ||||</span>}{preview.showQr!=='false'&&<span>[ QR CODE ]</span>}<small>{preview.footer||'Thank you'}</small></div></section></div><section className="panel"><h2>Printers</h2><Table rows={rows} columns={[["name","Name"],["connectionType","Connection"],["protocol","Protocol"],["status","Status"],["lastSeenAt","Last seen"],["isDefault","Default"]]}/></section><section className="panel"><h2>Active templates</h2><Table rows={templates} columns={[["name","Name"],["kind","Kind"],["version","Version"],["active","Active"],["updatedAt","Updated"]]}/></section></>}
function Reports({data:d,request,run,token}:any){const{t,language}=useI18n();const[report,setReport]=useState(d[0]??{}),[drawReport,setDrawReport]=useState(d[1]??{}),[from,setFrom]=useState(()=>shiftDate(haitiToday(),-29)),[to,setTo]=useState(()=>haitiToday());useEffect(()=>{setReport(d[0]??{});setDrawReport(d[1]??{})},[d]);async function filter(e:FormEvent<HTMLFormElement>){e.preventDefault();if(from>to){await run(async()=>{throw new Error(t('report.invalidDates'))});return}const q=new URLSearchParams({from,to});const values=await run(()=>Promise.all([request(`/reports/sales?${q}`),request(`/reports/draws?${q}`)]));if(values){setReport(values[0]);setDrawReport(values[1])}}async function pdf(){const q=new URLSearchParams({from,to});const response=await fetch(`${API}/reports/sales.pdf?${q}`,{headers:{authorization:`Bearer ${token}`}});if(!response.ok)return;const url=URL.createObjectURL(await response.blob()),a=document.createElement("a");a.href=url;a.download=`lottivexa-sales-${from}-${to}.pdf`;a.click();URL.revokeObjectURL(url)}const sessions=(drawReport.byDraw??[]).map((x:Row)=>({...x,sessionLabel:x.session==='MORNING'?t('report.morning'):x.session==='EVENING'?t('report.evening'):x.session}));return <><section className="report-banner"><div><span className="eyebrow">{t('nav.reports')}</span><h2>{t('report.byDraw')}</h2><p>{t('report.fromToday')}</p></div></section><section className="panel"><form className="inline report-filter" onSubmit={filter}><label>{t('report.from')}<input name="from" type="date" value={from} onChange={e=>setFrom(e.target.value)} required/></label><label>{t('report.to')}<input name="to" type="date" value={to} onChange={e=>setTo(e.target.value)} required/></label><button>{t('report.show')}</button><button type="button" className="secondary" onClick={pdf}>{t('report.download')}</button></form></section><div className="cards report-cards"><article className="metric-sales"><span>{t('report.sales')}</span><strong>{money(report.tickets?.sales)}</strong></article><article><span>{t('report.tickets')}</span><strong>{report.tickets?.count??0}</strong></article><article><span>{t('report.payouts')}</span><strong>{money(report.payouts?.amount)}</strong></article><article><span>{t('report.net')}</span><strong>{money(Number(report.tickets?.sales??0)-Number(report.payouts?.amount??0))}</strong></article><article><span>{t('report.commissions')}</span><strong>{money(report.commission)}</strong></article><article><span>{language==='fr'?'Gains potentiels':'Gany posib'}</span><strong>{money(report.tickets?.potentialWin)}</strong></article></div><div className="split report-breakdowns"><section className="panel"><h2>{t('report.byStatus')}</h2><Table rows={report.byStatus??[]} columns={[["status",t('report.status')],["count",t('report.count')],["amount",t('report.amount')]]}/></section><section className="panel"><h2>{t('report.byGame')}</h2><Table rows={report.byGame??[]} columns={[["gameName",t('report.lottery')],["count",t('report.count')],["amount",t('report.amount')]]}/></section><section className="panel"><h2>{t('report.byBranch')}</h2><Table rows={report.byBranch??[]} columns={[["branchName",t('report.branch')],["count",t('report.count')],["amount",t('report.amount')]]}/></section><section className="panel draw-breakdown"><h2>{t('report.byDraw')}</h2><div className="draw-report-list">{sessions.length?sessions.map((draw:Row)=><article key={draw.drawId}><div><strong>{draw.gameName}</strong><span>{draw.drawNumber}</span></div><span className={`session-pill ${draw.session==='MORNING'?'morning':'evening'}`}>{draw.sessionLabel}</span><b>{money(draw.amount)}</b><small>{draw.count} {t('report.tickets').toLowerCase()}</small></article>):<p className="empty">{t('report.noDraws')}</p>}</div></section></div></>}
function Branding({data:d,submit,request,load,has}:any){const[s={},domains=[]]=d;return <><div className="split"><section className="panel"><h2>Business settings</h2><form className="form one" onSubmit={e=>submit(e,'/settings',(x:Row)=>x,'PUT','Settings sove.')}><label>Currency<input name="currency" defaultValue={s.settings?.currency??'HTG'} minLength={3} maxLength={3} required/></label><label>Timezone<input name="timezone" defaultValue={s.settings?.timezone??'America/Port-au-Prince'} required/></label><label>Locale<input name="locale" defaultValue={s.settings?.locale??'ht-HT'} required/></label><label>Date format<input name="dateFormat" defaultValue={s.settings?.dateFormat??'DD/MM/YYYY'} required/></label><button>Save settings</button></form></section><section className="panel"><h2>Branding</h2><form className="form one" onSubmit={e=>submit(e,'/settings/branding',(x:Row)=>x,'PUT','Branding sove.')}><label>Business name<input name="businessName" defaultValue={s.branding?.businessName} required/></label><label>Logo HTTPS URL<input name="logoUrl" defaultValue={s.branding?.logoUrl}/></label><label>Favicon HTTPS URL<input name="faviconUrl" defaultValue={s.branding?.faviconUrl}/></label><label>Primary color<input name="primaryColor" type="color" defaultValue={s.branding?.primaryColor??'#172554'}/></label><label>Secondary color<input name="secondaryColor" type="color" defaultValue={s.branding?.secondaryColor??'#f59e0b'}/></label><button disabled={!has('custom_branding')}>Save branding</button></form></section></div><section className="panel"><h2>Custom domains</h2><form className="inline" onSubmit={e=>submit(e,'/domains')}><input name="domain" placeholder="portal.customer.com" required/><button disabled={!has('custom_domain')}>Add domain</button></form>{domains.map((x:Row)=><article className="domain" key={x.id}><div><b>{x.domain}</b><small>{x.verificationStatus} Â· SSL {x.sslStatus}{x.isPrimary?' Â· PRIMARY':''}</small><code>TXT _lottivexa.{x.domain} = {x.verificationToken}</code></div><div className="actions"><button onClick={async()=>{await request(`/domains/${x.id}/verify`,{method:'POST'});await load('branding')}}>Verify</button>{x.verificationStatus==='VERIFIED'&&!x.isPrimary&&<button onClick={async()=>{await request(`/domains/${x.id}/primary`,{method:'PATCH'});await load('branding')}}>Primary</button>}</div></article>)}</section></>}
function Audit({data:d}:{data:any[]}){const result=d[0]??{};return <section className="panel"><Table rows={result.items??result} columns={[['timestamp','Time'],['action','Action'],['entityType','Entity'],['entityId','Entity ID'],['ipAddress','IP']]}/></section>}

function LotterySetup({games,draws,request,load,can}:{games:Row[];draws:Row[];request:(path:string,init?:RequestInit)=>Promise<any>;load:(tab?:Tab)=>Promise<void>;can:(permission:string)=>boolean}){
 const[message,setMessage]=useState('');
 if(!can('settings.edit'))return null;
 async function execute(work:()=>Promise<void>,success:string){setMessage('');try{await work();setMessage(success);await load('lottery')}catch(error){setMessage(error instanceof Error?error.message:String(error))}}
 async function createBetType(e:FormEvent<HTMLFormElement>){e.preventDefault();const target=e.currentTarget,x=val(target);await execute(async()=>{const bet=await request('/lottery/bet-types',{method:'POST',body:JSON.stringify({code:String(x.code).toUpperCase(),name:x.name,selectionCount:Number(x.selectionCount),numberMin:Number(x.numberMin),numberMax:Number(x.numberMax),allowRepeats:x.allowRepeats==='true'})});await request(`/lottery/games/${x.gameId}/bet-types/${bet.id}`,{method:'POST',body:'{}'});target.reset()},'Bet type kreye epi konekte ak game lan.')}
 async function createOdds(e:FormEvent<HTMLFormElement>){e.preventDefault();const target=e.currentTarget,x=val(target),[gameId,betTypeId]=String(x.pair).split('|');await execute(async()=>{await request('/lottery/odds',{method:'POST',body:JSON.stringify({gameId,betTypeId,multiplier:String(x.multiplier),...(x.resultPosition?{resultPosition:Number(x.resultPosition)}:{}),startsAt:new Date(String(x.startsAt)).toISOString()})});target.reset()},'Odds yo aktive.')}
 async function installCatalog(){await execute(()=>request('/lottery/catalog/install',{method:'POST',body:'{}'}),'KatalÃ²g bolet Ayiti a enstale san doublon.')}
 async function toggleCatalog(game:Row){await execute(()=>request(`/lottery/catalog/${encodeURIComponent(game.catalogCode)}`,{method:'PATCH',body:JSON.stringify({enabled:game.status!=='ACTIVE'})}),game.status==='ACTIVE'?'Lotri a fÃ¨men.':'Lotri a aktive.')}
 const catalog=games.filter(game=>game.catalogCode);
 const attached=games.flatMap(game=>(game.betTypes??[]).map((entry:Row)=>({game,bet:entry.betType})));
 return <><section className="panel"><div className="title"><div><h2>KatalÃ²g bolet Ayiti</h2><p className="muted">Lotri, kalite jwÃ¨t, odds ak orÃ¨ debaz yo enstale san doublon. Ou ka modifye oswa fÃ¨men chak lotri.</p></div><button onClick={installCatalog}>Enstale / mete katalÃ²g ajou</button></div><div className="cards">{catalog.map(game=><article key={game.id}><span>{game.catalogCode}</span><strong>{game.name}</strong><small>{game.schedules?.length??0} orÃ¨ Â· {game.status} Â· {game.sourceVerifiedAt?"SOUS VERIFYE":"MANUAL"}</small><button className={game.status==="ACTIVE"?"danger":""} onClick={()=>toggleCatalog(game)}>{game.status==="ACTIVE"?"FÃ¨men":"Aktive"}</button><form className="inline" onSubmit={e=>{e.preventDefault();const logoUrl=String(new FormData(e.currentTarget).get("logoUrl")??"");void execute(()=>request(`/lottery/games/${game.id}`,{method:"PATCH",body:JSON.stringify({logoUrl})}),"Logo lotri a sove.")}}><input name="logoUrl" type="url" defaultValue={game.logoUrl} placeholder="HTTPS logo ofisyÃ¨l ki gen lisans" required/><button>Sove logo</button></form></article>)}</div>{!catalog.length&&<p className="empty">Klike Enstale pou mete katalÃ²g la nan tenant sa a.</p>}<h3>OrÃ¨ tiraj yo</h3>{catalog.flatMap(game=>(game.schedules??[]).map((schedule:Row)=>({...schedule,gameName:game.name}))).map((schedule:Row)=><article className="domain" key={schedule.id}><div><b>{schedule.gameName}</b><small>Jou {schedule.weekday} Â· fÃ¨men {schedule.closesAt} Â· rezilta {schedule.resultAt} Â· {schedule.active?"AKTIF":"FÃˆMEN"}</small></div><button className={schedule.active?"danger":""} onClick={()=>execute(()=>request(`/lottery/schedules/${schedule.id}`,{method:"PATCH",body:JSON.stringify({enabled:!schedule.active})}),schedule.active?"Tiraj la fÃ¨men.":"Tiraj la aktive.")}>{schedule.active?"FÃ¨men tiraj":"Aktive tiraj"}</button></article>)}</section><ManualResults draws={draws as any} request={request} reload={()=>load('lottery')}/><div className="split"><section className="panel"><h2>Create bet type</h2><form className="form one" onSubmit={createBetType}><label>Game<select name="gameId" required><option value="">Select game</option>{games.map(game=><option key={game.id} value={game.id}>{game.name}</option>)}</select></label><label>Code<input name="code" placeholder="STRAIGHT" pattern="[A-Za-z0-9_-]{2,40}" required/></label><label>Name<input name="name" placeholder="Straight number" required/></label><label>Selection count<input name="selectionCount" type="number" min="1" defaultValue="1" required/></label><label>Minimum number<input name="numberMin" type="number" min="0" defaultValue="0" required/></label><label>Maximum number<input name="numberMax" type="number" min="0" defaultValue="99" required/></label><label>Allow repeats<select name="allowRepeats" defaultValue="false"><option value="false">No</option><option value="true">Yes</option></select></label><button disabled={!games.length}>Create & attach bet type</button></form></section><section className="panel"><h2>Règ peman pa jwèt ak pozisyon</h2><p className="muted">Admin tenant lan fikse miltiplikatè pou chak jwèt. Pou Bolet, chwazi 1ye, 2yèm oswa 3yèm. Chanjman sa yo aplike sèlman sou nouvo tikè; tikè ki deja vann kenbe peman yo.</p><form className="form one" onSubmit={createOdds}><label>Game / bet type<select name="pair" required><option value="">Select</option>{attached.map(x=><option key={`${x.game.id}|${x.bet.id}`} value={`${x.game.id}|${x.bet.id}`}>{x.game.name} â€” {x.bet.name}</option>)}</select></label><label>Pozisyon Bolet<select name="resultPosition" defaultValue=""><option value="">Règ jeneral / lòt jwèt</option><option value="1">1ye</option><option value="2">2yèm</option><option value="3">3yèm</option></select></label><label>Miltiplikatè peman ($ pa $ jwe)<input name="multiplier" type="number" min="0.0001" step="0.0001" placeholder="50" required/></label><label>Effective from<input name="startsAt" type="datetime-local" required/></label><button disabled={!attached.length}>Activate odds</button></form></section></div>{message&&<p className="message">{message}</p>}</>
}

