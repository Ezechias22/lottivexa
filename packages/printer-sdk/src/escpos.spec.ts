import{describe,expect,it}from'vitest';import{EscPosDriver,renderTicket}from'./escpos';
const data={businessName:'King Lotto',ticketNumber:'LV-1',merchant:'Jean Pierre',branch:'Centre Ville',game:'NY',draw:'MID',lines:[{selection:'12-34',stake:'10.00',potentialWin:'500.00'}],amount:'10.00',potentialWin:'500.00',createdAt:'2026-01-01',barcode:'ABC',qrCode:'LV1:ABC'};
describe('ESC/POS',()=>{it('renders tenant branding without merchant name or platform name',()=>{const bytes=renderTicket({paperWidth:58,showBarcode:true,showQr:true,header:'LOTTIVEXA'},data),text=new TextDecoder().decode(bytes);expect(text).toContain('King Lotto');expect(text).toContain('TIKE LV-1');expect(text).not.toContain('Jean Pierre');expect(text).not.toContain('LOTTIVEXA');expect([...bytes].some((x,i,a)=>x===0x1d&&a[i+1]===0x6b&&a[i+2]===73)).toBe(true);expect([...bytes].some((x,i,a)=>x===0x1d&&a[i+1]===0x28&&a[i+2]===0x6b)).toBe(true)});it('rejects missing or platform branding',()=>{expect(()=>renderTicket({paperWidth:58,showBarcode:false,showQr:false},{...data,businessName:''})).toThrow('RECEIPT_BUSINESS_NAME_REQUIRED');expect(()=>renderTicket({paperWidth:58,showBarcode:false,showQr:false},{...data,businessName:'LOTTIVEXA'})).toThrow('RECEIPT_BUSINESS_NAME_REQUIRED')});it('omits machine codes when the template disables them',()=>{const bytes=[...renderTicket({paperWidth:58,showBarcode:false,showQr:false},data)];expect(bytes.some((x,i)=>x===0x1d&&bytes[i+1]===0x6b)).toBe(false)});it('writes through injected transport',async()=>{const writes:Uint8Array[]=[];const driver=new EscPosDriver({open:async()=>{},close:async()=>{},write:async d=>{writes.push(d)}});await driver.connect();await driver.testPrint();expect(writes.length).toBe(2)})});
