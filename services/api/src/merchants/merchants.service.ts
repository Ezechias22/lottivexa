import{BadRequestException,ForbiddenException,Injectable}from'@nestjs/common';import{prisma,Prisma}from'@lottivexa/database';import*as argon2 from'argon2';import type{Principal}from'../common/guards/jwt-auth.guard';import{merchantUpdate,MerchantUpdate}from'./merchant-update-policy';
export const merchantPermissions=['tickets.view','tickets.create','tickets.cancel','tickets.reprint','tickets.validate','tickets.pay','sales.view','sales.create','finance.view','finance.close','reports.view','printers.view'];
@Injectable()export class MerchantsService{
 async me(u:Principal){const tenantId=this.tenant(u),merchant=await prisma.merchantAccount.findFirst({where:{tenantId,userId:u.sub,status:'ACTIVE'},include:{branch:true,user:{select:{username:true,phone:true,email:true}},devices:true}});if(!merchant)throw new ForbiddenException('MERCHANT_ACCOUNT_REQUIRED');return merchant}
 async dashboard(u:Principal){const merchant=await this.me(u),from=new Date();from.setUTCHours(0,0,0,0);const[branding,tickets,payouts,session,recent]=await Promise.all([prisma.tenantBranding.findUnique({where:{tenantId:merchant.tenantId},select:{businessName:true}}),prisma.ticket.aggregate({where:{tenantId:merchant.tenantId,merchantId:merchant.id,createdAt:{gte:from},status:{notIn:['CANCELLED','VOID']}},_count:{_all:true},_sum:{amount:true,commission:true}}),prisma.payout.aggregate({where:{tenantId:merchant.tenantId,paidAt:{gte:from},ticket:{merchantId:merchant.id}},_count:{_all:true},_sum:{amount:true}}),prisma.cashSession.findFirst({where:{tenantId:merchant.tenantId,merchantId:merchant.id,status:'OPEN'},include:{movements:true}}),prisma.ticket.findMany({where:{tenantId:merchant.tenantId,merchantId:merchant.id},orderBy:{createdAt:'desc'},take:12,include:{winning:true,lines:{select:{isWinner:true}}}})]);return{businessName:branding?.businessName??'Bolet',merchant:{id:merchant.id,displayName:merchant.displayName,merchantNumber:merchant.merchantNumber,branch:merchant.branch,devices:merchant.devices},sales:tickets._sum.amount?.toString()??'0',commission:tickets._sum.commission?.toString()??'0',ticketCount:tickets._count._all,payouts:payouts._sum.amount?.toString()??'0',payoutCount:payouts._count._all,session,recent}}
 list(u:Principal){return prisma.merchantAccount.findMany({where:{tenantId:this.tenant(u),archivedAt:null},include:{user:{select:{id:true,username:true,email:true,phone:true,status:true,forcePasswordChange:true}},branch:{select:{id:true,code:true,name:true}}},orderBy:{displayName:'asc'}})}
 async create(u:Principal,dto:{displayName:string;merchantNumber:string;username:string;email?:string;phone?:string;temporaryPassword:string;branchId:string}){const tenantId=this.tenant(u),branch=await prisma.branch.findFirst({where:{id:dto.branchId,tenantId,status:'ACTIVE',archivedAt:null}});if(!branch)throw new BadRequestException('INVALID_BRANCH');await this.limit(tenantId);const hash=await argon2.hash(dto.temporaryPassword,{type:argon2.argon2id});return prisma.$transaction(async tx=>{let role=await tx.role.findFirst({where:{tenantId,code:'MERCHANT'}});if(!role)role=await tx.role.create({data:{tenantId,code:'MERCHANT',name:'Merchant',isSystem:true}});const permissions=await tx.permission.findMany({where:{code:{in:merchantPermissions}}});await tx.rolePermission.createMany({data:permissions.map(p=>({roleId:role!.id,permissionId:p.id})),skipDuplicates:true});const user=await tx.user.create({data:{tenantId,username:dto.username,email:dto.email||null,phone:dto.phone||null,passwordHash:hash,status:'ACTIVE',forcePasswordChange:true,roles:{create:{roleId:role.id}}}});const merchant=await tx.merchantAccount.create({data:{tenantId,userId:user.id,branchId:branch.id,displayName:dto.displayName,merchantNumber:dto.merchantNumber,status:'ACTIVE'}});await tx.auditLog.create({data:{tenantId,userId:u.sub,action:'USER_CREATE',entityType:'MerchantAccount',entityId:merchant.id,newValues:{username:dto.username,branchId:branch.id,merchantNumber:dto.merchantNumber}}});return{id:merchant.id,userId:user.id,username:user.username,temporaryPasswordRequired:true}})}
 async update(u:Principal,id:string,input:MerchantUpdate){const tenantId=this.tenant(u),dto=merchantUpdate(input),current=await prisma.merchantAccount.findFirst({where:{id,tenantId,archivedAt:null},include:{user:true}});if(!current)throw new ForbiddenException('RESOURCE_NOT_FOUND');const branchId=dto.branchId??current.branchId;if(dto.branchId&&!await prisma.branch.findFirst({where:{id:branchId,tenantId,status:'ACTIVE',archivedAt:null}}))throw new BadRequestException('INVALID_BRANCH');const loginChanged=dto.username!==undefined||dto.email!==undefined||dto.phone!==undefined;await prisma.$transaction(async tx=>{await tx.merchantAccount.update({where:{id},data:{displayName:dto.displayName,branchId}});if(branchId!==current.branchId)await tx.device.updateMany({where:{tenantId,merchantId:id},data:{branchId}});if(loginChanged){await tx.user.update({where:{id:current.userId},data:{username:dto.username,email:dto.email===undefined?undefined:dto.email||null,phone:dto.phone===undefined?undefined:dto.phone||null,tokenVersion:{increment:1}}});await tx.refreshToken.updateMany({where:{userId:current.userId,revokedAt:null},data:{revokedAt:new Date()}})}await tx.auditLog.create({data:{tenantId,userId:u.sub,action:'UPDATE',entityType:'MerchantAccount',entityId:id,oldValues:{displayName:current.displayName,branchId:current.branchId,username:current.user.username,email:current.user.email,phone:current.user.phone}as Prisma.InputJsonValue,newValues:{...dto,branchId}as Prisma.InputJsonValue}})});return prisma.merchantAccount.findFirstOrThrow({where:{id,tenantId},include:{user:{select:{username:true,email:true,phone:true,status:true}},branch:true}})}
 async disable(u:Principal,id:string){const tenantId=this.tenant(u),merchant=await prisma.merchantAccount.findFirst({where:{id,tenantId}});if(!merchant)throw new ForbiddenException('RESOURCE_NOT_FOUND');await prisma.$transaction([prisma.merchantAccount.update({where:{id},data:{status:'DISABLED'}}),prisma.user.update({where:{id:merchant.userId},data:{status:'DISABLED',tokenVersion:{increment:1}}}),prisma.refreshToken.updateMany({where:{userId:merchant.userId,revokedAt:null},data:{revokedAt:new Date()}}),prisma.auditLog.create({data:{tenantId,userId:u.sub,action:'USER_DISABLE',entityType:'MerchantAccount',entityId:id}})]);return{disabled:true}}
 private async limit(tenantId:string){const now=new Date(),sub=await prisma.subscription.findFirstOrThrow({where:{tenantId,OR:[{status:{in:['ACTIVE','TRIAL']},currentPeriodEndsAt:{gt:now}},{status:'PAST_DUE',graceEndsAt:{gt:now}}]},include:{plan:true},orderBy:{createdAt:'desc'}});if(sub.plan.maxMerchants!==null&&await prisma.merchantAccount.count({where:{tenantId,archivedAt:null}})>=sub.plan.maxMerchants)throw new ForbiddenException('LIMIT_REACHED')}
 private tenant(u:Principal){if(!u.tenantId)throw new ForbiddenException('TENANT_ACCESS_REQUIRED');return u.tenantId}
}
