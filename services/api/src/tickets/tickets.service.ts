import{BadRequestException,ForbiddenException,Injectable}from'@nestjs/common';import{prisma,Prisma,TicketStatus}from'@lottivexa/database';import{randomBytes}from'node:crypto';import type{Principal}from'../common/guards/jwt-auth.guard';import{isBettingOpen}from'../lottery/lottery-policy';import{cancellationDeadline,priceLines,validateHaitianBetType}from'./ticket-policy';
@Injectable()export class TicketsService{async create(u:Principal,dto:{drawId:string;idempotencyKey:string;deviceId?:string;lines:{betTypeId:string;selection:number[];stake:string}[]}){const tenantId=this.tenant(u);const existing=await prisma.ticket.findUnique({where:{tenantId_idempotencyKey:{tenantId,idempotencyKey:dto.idempotencyKey}},include:{lines:true}});if(existing)return existing;const merchant=await prisma.merchantAccount.findFirst({where:{tenantId,userId:u.sub,status:'ACTIVE',branch:{status:'ACTIVE'}}});if(!merchant)throw new ForbiddenException('MERCHANT_ACCOUNT_REQUIRED');if(dto.deviceId){const device=await prisma.device.findFirst({where:{id:dto.deviceId,tenantId,branchId:merchant.branchId,merchantId:merchant.id,status:{in:['ONLINE','OFFLINE']}}});if(!device)throw new ForbiddenException('DEVICE_NOT_AUTHORIZED')}const draw=await prisma.draw.findFirst({where:{id:dto.drawId,tenantId},include:{game:true}});if(!draw||!isBettingOpen(draw.status,draw.closesAt,draw.game.cutoffSeconds))throw new BadRequestException('DRAW_CLOSED');const betIds=[...new Set(dto.lines.map(l=>l.betTypeId))];const configured=await prisma.gameBetType.findMany({where:{gameId:draw.gameId,betTypeId:{in:betIds},active:true},include:{betType:true}});if(configured.length!==betIds.length)throw new BadRequestException('INVALID_BET_TYPE');const odds=await prisma.oddsRule.findMany({where:{tenantId,gameId:draw.gameId,betTypeId:{in:betIds},active:true,startsAt:{lte:new Date()},OR:[{endsAt:null},{endsAt:{gt:new Date()}}]},orderBy:{startsAt:'desc'}});const priced=priceLines(dto.lines.map(line=>{const bt=configured.find(x=>x.betTypeId===line.betTypeId)!.betType;const odd=odds.find(x=>x.betTypeId===line.betTypeId);if(!odd)throw new BadRequestException('ODDS_NOT_CONFIGURED');validateHaitianBetType(bt.code,line.selection);return{...line,odds:odd.multiplier.toString(),selectionCount:bt.selectionCount,numberMin:bt.numberMin,numberMax:bt.numberMax,allowRepeats:bt.allowRepeats}}));const amount=priced.reduce((s,l)=>s.add(l.stake),new Prisma.Decimal(0));const potentialWin=priced.reduce((s,l)=>s.add(l.potentialWin),new Prisma.Decimal(0));await this.checkLimits(tenantId,draw.id,draw.gameId,merchant.id,priced);const token=randomBytes(12).toString('hex').toUpperCase();const date=new Date(),day=`${String(date.getUTCFullYear()).slice(-2)}${String(date.getUTCMonth()+1).padStart(2,'0')}${String(date.getUTCDate()).padStart(2,'0')}`,suffix=(randomBytes(4).readUInt32BE(0)%60466176).toString(36).toUpperCase().padStart(5,'0');const ticketNumber=`${day}${suffix}`;return prisma.$transaction(async tx=>{await tx.$executeRaw`SELECT pg_advisory_xact_lock(hashtextextended(${`${tenantId}:${dto.idempotencyKey}`}, 0))`;const concurrent=await tx.ticket.findUnique({where:{tenantId_idempotencyKey:{tenantId,idempotencyKey:dto.idempotencyKey}},include:{lines:true,events:true}});if(concurrent)return concurrent;const ticket=await tx.ticket.create({data:{tenantId,ticketNumber,idempotencyKey:dto.idempotencyKey,branchId:merchant.branchId,merchantId:merchant.id,deviceId:dto.deviceId,gameId:draw.gameId,drawId:draw.id,amount,potentialWin,barcode:token,qrCode:`LV1:${tenantId}:${token}`,lines:{create:priced.map(l=>({tenantId,betTypeId:l.betTypeId,selection:l.selection,selectionKey:l.selectionKey,stake:l.stake,odds:l.odds,potentialWin:l.potentialWin}))},events:{create:{tenantId,type:'CREATED',userId:u.sub,deviceId:dto.deviceId}}}});const cash=await tx.ledgerAccount.upsert({where:{tenantId_code:{tenantId,code:'MERCHANT_CASH'}},update:{},create:{tenantId,code:'MERCHANT_CASH',name:'Merchant cash',type:'ASSET'}});const sales=await tx.ledgerAccount.upsert({where:{tenantId_code:{tenantId,code:'TICKET_SALES'}},update:{},create:{tenantId,code:'TICKET_SALES',name:'Ticket sales',type:'REVENUE'}});await tx.ledgerTransaction.create({data:{tenantId,type:'SALE',referenceType:'Ticket',referenceId:ticket.id,idempotencyKey:`sale:${dto.idempotencyKey}`,entries:{create:[{tenantId,accountId:cash.id,debit:amount},{tenantId,accountId:sales.id,credit:amount}]}}});return tx.ticket.findUniqueOrThrow({where:{id:ticket.id},include:{lines:true,events:true}})},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable})}
 async cancel(u:Principal,ref:string){const tenantId=this.tenant(u),now=new Date(),merchant=await prisma.merchantAccount.findFirst({where:{tenantId,userId:u.sub,status:'ACTIVE'}});return prisma.$transaction(async tx=>{const ticket=await tx.ticket.findFirst({where:{tenantId,OR:[{ticketNumber:ref},{barcode:ref},{qrCode:ref}],...(merchant?{merchantId:merchant.id}:{})},include:{draw:true,payout:true}});if(!ticket)throw new ForbiddenException('INVALID_TICKET');if(ticket.status!=='VALID'||ticket.payout)throw new BadRequestException(ticket.status==='CANCELLED'?'TICKET_ALREADY_CANCELLED':'INVALID_TICKET_STATUS');if(ticket.draw.status!=='OPEN'||now>=ticket.draw.closesAt)throw new BadRequestException('DRAW_CLOSED');const rule=await tx.lotteryRule.findFirst({where:{tenantId,key:'ticket_cancellation_seconds',active:true,effectiveFrom:{lte:now},OR:[{effectiveTo:null},{effectiveTo:{gt:now}}]},orderBy:{effectiveFrom:'desc'}}),configured=rule?.value&&typeof rule.value==='object'&&!Array.isArray(rule.value)?Number((rule.value as Record<string,unknown>).seconds):Number(process.env.TICKET_CANCELLATION_SECONDS??300),deadline=cancellationDeadline(ticket.createdAt,ticket.draw.closesAt,configured);if(now>deadline)throw new BadRequestException('CANCELLATION_WINDOW_CLOSED');const changed=await tx.ticket.updateMany({where:{id:ticket.id,tenantId,status:'VALID'},data:{status:'CANCELLED'}});if(changed.count!==1)throw new BadRequestException('TICKET_CONCURRENTLY_MODIFIED');const originals=await tx.ledgerTransaction.findMany({where:{tenantId,referenceId:ticket.id,status:'POSTED',type:{in:['SALE','COMMISSION']}},include:{entries:true}});for(const original of originals){await tx.ledgerTransaction.update({where:{id:original.id},data:{status:'REVERSED'}});await tx.ledgerTransaction.create({data:{tenantId,type:'ADJUSTMENT',referenceType:'TicketCancellation',referenceId:ticket.id,idempotencyKey:`cancel:${original.id}`,entries:{create:original.entries.map(e=>({tenantId,accountId:e.accountId,debit:e.credit,credit:e.debit}))}}})}await tx.ticketEvent.createMany({data:[{tenantId,ticketId:ticket.id,type:'CANCEL_REQUESTED',userId:u.sub},{tenantId,ticketId:ticket.id,type:'CANCELLED',userId:u.sub,metadata:{deadline:deadline.toISOString()}}]});await tx.auditLog.create({data:{tenantId,userId:u.sub,action:'CANCEL',entityType:'Ticket',entityId:ticket.id,oldValues:{status:'VALID'},newValues:{status:'CANCELLED'}}});return tx.ticket.findUniqueOrThrow({where:{id:ticket.id},include:{lines:true,events:true}})},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable})}
 async get(u:Principal,ref:string){const tenantId=this.tenant(u),merchant=await prisma.merchantAccount.findFirst({where:{tenantId,userId:u.sub,status:'ACTIVE'}});const uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(ref);return prisma.ticket.findFirstOrThrow({where:{tenantId,...(merchant?{branchId:merchant.branchId}:{}),OR:[...(uuid?[{id:ref}]:[]),{ticketNumber:ref},{barcode:ref},{qrCode:ref}]},include:{lines:{include:{betType:true}},events:true,draw:{include:{game:true}},merchant:{include:{branch:true}},winning:true,payout:true}})}
 async search(u:Principal,q:{status?:TicketStatus;drawId?:string}){const tenantId=this.tenant(u),merchant=await prisma.merchantAccount.findFirst({where:{tenantId,userId:u.sub,status:'ACTIVE'}});return prisma.ticket.findMany({where:{tenantId,...(merchant?{merchantId:merchant.id}:{}),...(q.status?{status:q.status}:{}),...(q.drawId?{drawId:q.drawId}:{})},orderBy:{createdAt:'desc'},take:100})}
 private async checkLimits(tenantId:string,drawId:string,gameId:string,merchantId:string,lines:ReturnType<typeof priceLines>){const now=new Date();const limits=await prisma.bettingLimit.findMany({where:{tenantId,active:true,startsAt:{lte:now},OR:[{endsAt:null},{endsAt:{gt:now}}],AND:[{OR:[{scope:'TENANT'},{gameId},{drawId},{scope:'MERCHANT',scopeId:merchantId}]}]}});for(const line of lines)for(const limit of limits.filter(x=>!x.betTypeId||x.betTypeId===line.betTypeId).filter(x=>!x.numberKey||x.numberKey===line.selectionKey)){if(limit.minStake&&line.stake.lt(limit.minStake))throw new BadRequestException('BELOW_MINIMUM_STAKE');if(limit.maxStake&&line.stake.gt(limit.maxStake))throw new BadRequestException('LIMIT_REACHED')}}
 private tenant(u:Principal){if(!u.tenantId)throw new ForbiddenException('TENANT_ACCESS_REQUIRED');return u.tenantId}}
