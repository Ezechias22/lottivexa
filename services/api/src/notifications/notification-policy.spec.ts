import{describe,expect,it}from'vitest';import{canReceive}from'./notification-policy';describe('notification preferences',()=>{it('defaults in-app on and external channels off',()=>{expect(canReceive([],'PAYOUT','IN_APP')).toBe(true);expect(canReceive([],'PAYOUT','SMS')).toBe(false)});it('honors explicit preference',()=>expect(canReceive([{eventType:'PAYOUT',channel:'SMS',enabled:true}],'PAYOUT','SMS')).toBe(true))})
