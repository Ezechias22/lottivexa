import{describe,expect,it}from'vitest';import{assertDrawTimes,assertDrawTransition,isBettingOpen,validateSelection}from'./lottery-policy';
describe('lottery policy',()=>{it('allows only the draw state machine',()=>{expect(()=>assertDrawTransition('OPEN','CLOSED')).not.toThrow();expect(()=>assertDrawTransition('OPEN','RESULT_PUBLISHED')).toThrow('INVALID_DRAW_TRANSITION')});it('validates chronological draw times',()=>{expect(()=>assertDrawTimes(new Date('2026-01-01T10:00Z'),new Date('2026-01-01T09:00Z'),new Date('2026-01-01T11:00Z'))).toThrow('INVALID_DRAW_TIMES')});it('enforces cutoff before close',()=>{const close=new Date('2026-01-01T10:00Z');expect(isBettingOpen('OPEN',close,300,new Date('2026-01-01T09:54:59Z'))).toBe(true);expect(isBettingOpen('OPEN',close,300,new Date('2026-01-01T09:55:00Z'))).toBe(false)});it('validates count, range and duplicates',()=>{expect(()=>validateSelection([1,2],2,0,99,false)).not.toThrow();expect(()=>validateSelection([1,1],2,0,99,false)).toThrow('DUPLICATE_SELECTION');expect(()=>validateSelection([100],1,0,99,true)).toThrow('NUMBER_OUT_OF_RANGE')})});
