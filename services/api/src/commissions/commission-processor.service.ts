import{ForbiddenException,Injectable}from'@nestjs/common';import{prisma,Prisma}from'@lottivexa/database';import type{Principal}from'../common/guards/jwt-auth.guard';import{calculateCommission}from'./commission-policy';
@Injectable()export class CommissionProcessor{async apply(u:Principal,ticketId:string){if(!u.tenantId)throw new ForbiddenException('TENANT_ACCESS_REQUIRED');const tenantId=u.tenantId;const existing=await prisma.commissionTransaction.findUnique({where:{ticketId}});if(existing){if(existing.tenantId!==tenantId)throw new ForbiddenException('RESOURCE_NOT_FOUND');return existing}return prisma.$transaction(async tx=>{const ticket=await tx.ticket.findFirst({where:{id:ticketId,tenantId,status:{notIn:['CANCELLED','VOID']}}});if(!ticket)throw new ForbiddenException('RESOURCE_NOT_FOUND');const now=new Date();const rules=await tx.commissionRule.findMany({where:{tenantId,active:true,startsAt:{lte:now},OR:[{endsAt:null},{endsAt:{gt:now}}],AND:[{OR:[{scope:'MERCHANT',scopeId:ticket.merchantId},{scope:'BRANCH',scopeId:ticket.branchId},{scope:'GAME',scopeId:ticket.gameId},{scope:'TENANT'}]}]},orderBy:{priority:'desc'}});const rule=rules[0];if(!rule)throw new ForbiddenException('COMMISSION_RULE_NOT_FOUND');const amount=calculateCommission(rule.kind,ticket.amount,rule.percentage,rule.fixedAmount,rule.tiers);const expense=await tx.ledgerAccount.upsert({where:{tenantId_code:{tenantId,code:'COMMISSION_EXPENSE'}},update:{},create:{tenantId,code:'COMMISSION_EXPENSE',name:'Merchant commissions',type:'EXPENSE'}});const payable=await tx.ledgerAccount.upsert({where:{tenantId_code:{tenantId,code:'COMMISSION_PAYABLE'}},update:{},create:{tenantId,code:'COMMISSION_PAYABLE',name:'Commission payable',type:'LIABILITY'}});const ledger=await tx.ledgerTransaction.create({data:{tenantId,type:'COMMISSION',referenceType:'Ticket',referenceId:ticket.id,idempotencyKey:`commission:${ticket.id}`,entries:{create:[{tenantId,accountId:expense.id,debit:amount},{tenantId,accountId:payable.id,credit:amount}]}}});const commission=await tx.commissionTransaction.create({data:{tenantId,ticketId:ticket.id,merchantId:ticket.merchantId,ruleId:rule.id,baseAmount:ticket.amount,commissionAmount:amount,ledgerTransactionId:ledger.id}});await tx.ticket.update({where:{id:ticket.id},data:{commission:amount}});await tx.auditLog.create({data:{tenantId,userId:u.sub,action:'CREATE',entityType:'CommissionTransaction',entityId:commission.id,newValues:{ticketId:ticket.id,amount:amount.toString(),ruleId:rule.id}}});return commission},{isolationLevel:Prisma.TransactionIsolationLevel.Serializable})}}
