import{BadRequestException}from'@nestjs/common';import{CommissionKind,Prisma}from'@lottivexa/database';
export function calculateCommission(kind:CommissionKind,base:Prisma.Decimal,percentage?:Prisma.Decimal|null,fixed?:Prisma.Decimal|null,tiers?:unknown){if(kind==='PERCENTAGE'){if(!percentage||percentage.lt(0)||percentage.gt(100))throw new BadRequestException('INVALID_COMMISSION');return base.mul(percentage).div(100).toDecimalPlaces(4)}if(kind==='FIXED'){if(!fixed||fixed.lt(0))throw new BadRequestException('INVALID_COMMISSION');return Prisma.Decimal.min(base,fixed).toDecimalPlaces(4)}if(!Array.isArray(tiers))throw new BadRequestException('INVALID_COMMISSION_TIERS');const tier=(tiers as{upTo:string|null;percentage:string}[]).find(t=>t.upTo===null||base.lte(t.upTo));if(!tier)throw new BadRequestException('COMMISSION_TIER_NOT_FOUND');return base.mul(tier.percentage).div(100).toDecimalPlaces(4)}
