import{ForbiddenException,Injectable}from'@nestjs/common';import{Prisma,prisma}from'@lottivexa/database';import type{Principal}from'../common/guards/jwt-auth.guard';
@Injectable()export class SettingsService{async get(u:Principal){const tenantId=this.tenant(u),now=new Date();const [tenant,subscription]=await Promise.all([prisma.tenant.findUniqueOrThrow({where:{id:tenantId},select:{legalName:true,settings:true,branding:true}}),prisma.subscription.findFirst({where:{tenantId,OR:[{status:{in:['ACTIVE','TRIAL']},currentPeriodEndsAt:{gt:now}},{status:'PAST_DUE',graceEndsAt:{gt:now}}]},include:{plan:{include:{features:true}}},orderBy:{createdAt:'desc'}})]);return{...tenant,subscription}}async settings(u:Principal,dto:{currency:string;timezone:string;locale:string;dateFormat:string}){const tenantId=this.tenant(u),old=await prisma.tenantSetting.findUnique({where:{tenantId}});const value=await prisma.tenantSetting.upsert({where:{tenantId},update:{...dto,currency:dto.currency.toUpperCase()},create:{tenantId,...dto,currency:dto.currency.toUpperCase()}});await prisma.auditLog.create({data:{tenantId,userId:u.sub,action:'SETTINGS_CHANGE',entityType:'TenantSetting',entityId:tenantId,oldValues:old as unknown as Prisma.InputJsonValue,newValues:value as unknown as Prisma.InputJsonValue}});return value}async branding(u:Principal,dto:{businessName:string;logoUrl?:string;faviconUrl?:string;primaryColor:string;secondaryColor:string}){const tenantId=this.tenant(u),old=await prisma.tenantBranding.findUnique({where:{tenantId}});const value=await prisma.tenantBranding.upsert({where:{tenantId},update:dto,create:{tenantId,...dto}});await prisma.auditLog.create({data:{tenantId,userId:u.sub,action:'SETTINGS_CHANGE',entityType:'TenantBranding',entityId:tenantId,oldValues:old as unknown as Prisma.InputJsonValue,newValues:value as unknown as Prisma.InputJsonValue}});return value}private tenant(u:Principal){if(!u.tenantId)throw new ForbiddenException('TENANT_ACCESS_REQUIRED');return u.tenantId}}
