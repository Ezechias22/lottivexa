import{BadRequestException}from'@nestjs/common';import{Prisma}from'@lottivexa/database';export type JournalLine={accountId:string;debit?:string;credit?:string};export function validateJournal(lines:JournalLine[]){if(lines.length<2)throw new BadRequestException('LEDGER_REQUIRES_TWO_ENTRIES');let debit=new Prisma.Decimal(0),credit=new Prisma.Decimal(0);for(const line of lines){const d=new Prisma.Decimal(line.debit??0),c=new Prisma.Decimal(line.credit??0);if((d.gt(0)?1:0)+(c.gt(0)?1:0)!==1)throw new BadRequestException('INVALID_LEDGER_ENTRY');debit=debit.add(d);credit=credit.add(c)}if(!debit.eq(credit))throw new BadRequestException('UNBALANCED_LEDGER');return{debit,credit}}
