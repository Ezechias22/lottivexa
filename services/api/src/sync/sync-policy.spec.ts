import{describe,expect,it}from'vitest';import{canonicalJson,mutationHash}from'./sync-policy';describe('sync canonicalization',()=>{it('hashes equivalent key orders identically',()=>{expect(mutationHash({b:2,a:{y:2,x:1}})).toBe(mutationHash({a:{x:1,y:2},b:2}))});it('detects changed payloads',()=>expect(mutationHash({stake:'10'})).not.toBe(mutationHash({stake:'11'})));it('preserves array order',()=>expect(canonicalJson([2,1])).not.toBe(canonicalJson([1,2])))});
