import{describe,expect,it}from'vitest';import{deliveryTarget,nextSubscriptionStatus,retryDelay}from'./jobs-policy';describe('background policy',()=>{const now=new Date('2026-09-06T00:00:00Z');it('moves expired subscriptions through grace then suspension',()=>{expect(nextSubscriptionStatus(new Date('2026-09-05'),null,now)).toBe('PAST_DUE');expect(nextSubscriptionStatus(new Date('2026-09-01'),new Date('2026-09-05'),now)).toBe('SUSPENDED')});it('bounds retry backoff',()=>expect(retryDelay(99)).toBe(3600));it('routes each channel to its real destination',()=>{const user={email:'owner@example.com',phone:'+50912345678'};expect(deliveryTarget('EMAIL',user,['push-a'])).toEqual(['owner@example.com']);expect(deliveryTarget('SMS',user,['push-a'])).toEqual(['+50912345678']);expect(deliveryTarget('PUSH',user,['push-a','push-b'])).toEqual(['push-a','push-b'])});it('never substitutes a phone for a missing push token',()=>expect(deliveryTarget('PUSH',{email:null,phone:'+50912345678'},[])).toEqual([]))})
