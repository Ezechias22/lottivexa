import{BadRequestException,ForbiddenException,Injectable}from'@nestjs/common';import{prisma}from'@lottivexa/database';import type{Principal}from'../common/guards/jwt-auth.guard';
@Injectable()export class RolesService{list(u:Principal){const tenantId=this.tenant(u);return prisma.role.findMany({where:{tenantId},include:{permissions:{include:{permission:true}}},orderBy:{name:'asc'}})}async create(u:Principal,dto:{code:string;name:string;permissions:string[]}){const tenantId=this.tenant(u);const permissions=await prisma.permission.findMany({where:{code:{in:dto.permissions}}});if(permissions.length!==new Set(dto.permissions).size)throw new BadRequestException('INVALID_PERMISSION');return prisma.$transaction(async tx=>{const role=await tx.role.create({data:{tenantId,code:dto.code,name:dto.name,permissions:{create:permissions.map(p=>({permissionId:p.id}))}}});await tx.auditLog.create({data:{tenantId,userId:u.sub,action:'CREATE',entityType:'Role',entityId:role.id,newValues:{code:dto.code,permissions:dto.permissions}}});return role})}private tenant(u:Principal){if(!u.tenantId)throw new ForbiddenException('TENANT_ACCESS_REQUIRED');return u.tenantId}}
